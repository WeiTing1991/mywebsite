{
  "name": "weitingworkweb",
  "version": "1.0.0",
  "description": "This is website that showcase my works. Front-end build with HTMX and TailwindCSS. Back-end with go and templ lib.",
  "main": "n",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tailwindcss build -o static/css/tailwind.css",
    "watch": "tailwindcss build -o static/css/tailwind.css --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "netlify-cli": "^17.22.1",
    "tailwindcss": "^3.4.17"
  }
}
